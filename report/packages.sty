% ********** PRIMARY
\usepackage{xcolor}

% Set latex's output font encoding to T1 (Standard)
\usepackage[T1]{fontenc}

% Set file input encoding to utf8 (Standard)
\usepackage[utf8]{inputenc}

% Include geometry package (Standard)
% With verbose, geometry dumps it's core variables into the .log file
%\usepackage[verbose]{geometry}

% Use latin modern font
\usepackage{lmodern}

% Include textcomp for extra text symbols
\usepackage{textcomp}

% Load microtype package (Standard)
\usepackage{microtype}

% Include calc for infix notation arithmetic (Standard)
\usepackage{calc}

% Include \syntaxonly to suppress output and check syntax
\usepackage{syntonly}

% Include subfiles to ease project layout into multiple files
\usepackage{subfiles}

% Use biber of bibliography
\usepackage[backend=biber,sortcites=true,style=ieee]{biblatex}

% Include images with \includegraphics[scale=*]{file}
\usepackage{graphicx}

% Include captions (Standard)
\usepackage[centerlast]{caption}

% Manipulate headers and footnotes
\usepackage{fancyhdr}

%% ********** SECONDARY

% Include underline utility \uline
\usepackage[normalem]{ulem}

% Include inconsolata monospace font
\usepackage{inconsolata}

% Improve tabular environment
\usepackage{tabularx}

% Stop floats from crossing sections
\usepackage[section]{placeins}

% Customize lists, in particular enumerate
\usepackage{enumitem}

% Float figures left and right with environment wrapfigure
\usepackage{wrapfig}

% Make cells inside tabels
\usepackage{makecell}

% ********** MATH PACKAGES

% Include AMS's math utils (Standard)
\usepackage{amsmath}
   
% Enhance amsmath (Standard)
\usepackage{mathtools}

% Include extra math symbols (Standard)
\usepackage{amssymb}

% Include extra math fonts (such as BlackBoard) (Standard)
\usepackage{amsfonts}

% Include AMS's theorem utility (includes proof environment)
\usepackage{amsthm}

% Algorithm environments to write algorithms!
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}

% Include babel (Standard)
% The primary language is the one on the right
\usepackage[portuguese,english]{babel}

% Manipulation of titles (chapters, sections, ...)
\usepackage[explicit,indentafter,nobottomtitles*]{titlesec}

% Include links to pages from Table of Contents
\usepackage[linktocpage=true]{hyperref}

% Clever cross-references, must be included after hyperref
\usepackage{cleveref}

%\usepackage{times}

% ********** SETUP
\graphicspath{{./images/}{./}{./contents/}}
